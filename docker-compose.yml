services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./prisma:/app/prisma
      - ./storage/uploads:/app/storage/uploads
    environment:
      - DATABASE_URL=file:./dev.db
      - NODE_ENV=production
    restart: unless-stopped
